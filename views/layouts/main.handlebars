<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Textn Tech</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
  <div class="container max-w-full h-screen">
    <header class="flex justify-between pt-5 px-5 h-40 bg-gradient-to-r from-sky-500 to-gray-500 sticky top-0">
      <h1 class="order-2">
        <a href="/" class="text-6xl no-underline text-slate-300 font-bold font-serif ">Textn Tech</a>
      </h1>
      <nav class="order-1">
        <a href="/" class="text-2xl px-2 hover:bg-sky-700 ease-in duration-300 font-medium">Home</a>
        <a href="/dashboard" class="text-2xl px-2 hover:bg-sky-700 duration-300 font-medium">Dashboard</a>
        {{#if loggedIn}}
        <a id="logout" class="text-2xl px-2 hover:bg-sky-700 duration-300 font-medium">logout</a>
        {{else}}
         <a href="/login" class="text-2xl px-2 hover:bg-sky-700 duration-300 font-medium">login</a>
         {{/if}}
      </nav>
    </header>
    <main class="h-full">
      {{{ body }}}
    </main>
  </div>
  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>

